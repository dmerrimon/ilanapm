# Task Ontology v4.0 - Tasks Migrated to Database
# All task definitions have been migrated to the timeline_templates database
# Regulatory authority data is now in authorities.yaml and regulatory_workflows.yaml

metadata:
  version: "4.0"
  created: "2026-02-13"
  description: "Task definitions migrated to database. Use timeline_templates API for task data."
  migration_note: "As of v4.0, all tasks are stored in the database (timeline_templates, template_tasks, template_dependencies tables). This file is kept for backward compatibility but contains no task data."

# Tasks migrated to database - see timeline_templates API
# GET /api/v1/templates/ to retrieve templates
tasks: []

# For regulatory authority data, see:
# - authorities.yaml: Comprehensive authority definitions (60+ authorities across 23 countries)
# - regulatory_workflows.yaml: Country-specific regulatory workflows and submission requirements
